{
  "name": "webxterm",
  "version": "1.0.0",
  "description": "Zero-install web SSH workspace. Open a browser, generate or import a key, connect to any server — terminal, file explorer, uploads, remote editing, nothing to install on either end.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
