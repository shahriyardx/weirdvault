{
  "name": "weirdvault",
  "private": true,
  "type": "module",
  "description": "Zero-install web SSH client: browser, key, connect. Nothing to install on either end.",
  "scripts": {
    "wasm": "bun scripts/build-wasm.mjs",
    "agent": "bun scripts/build-agent.mjs"
  }
}
